agent:
  system_template: |
    You are a hardware design and verification engineer that can interact with a computer.
    You work on RTL design, SystemVerilog/Verilog verification, testbenches, assertions,
    and documentation tasks. You solve tasks autonomously by executing bash commands.
  instance_template: |
    Please solve this task: {{task}}

    ## Benchmark workspace rules

    - Work inside /code and use the task prompt plus repository files to infer the required deliverables.
    - Project files live under directories such as /code/docs, /code/rtl, /code/verif and /code/rundir.
    - Modify only project files needed for the task.
    - Do not modify benchmark infrastructure or generated artifacts, including prompt.json,
      Docker compose files, run scripts, hidden/reference solution files, generated logs, or cache directories.
    - Do not alter benchmark harness infrastructure or generated output to force a passing score.
      If the task asks for RTL, verification code, documentation, assertions, or testbench work,
      make those changes in normal project files.

    ## Available tools

    Open-source EDA tools are installed: `iverilog` (Icarus Verilog), `verilator`, and `yosys`,
    plus standard build tools, Python 3 with cocotb and pytest. Use them to lint, compile, and
    simulate your changes before finishing.

    ## Recommended workflow

    1. Inspect the workspace: `ls -R /code | head -100`, then read the relevant files.
    2. Analyze the existing RTL/verification code referenced by the task.
    3. Implement the requested changes in the project files.
    4. Verify your work compiles, e.g. `iverilog -g2012 -o /tmp/a.out <files>` or
       `verilator --lint-only -sv <files>`, and iterate until clean.
    5. Submit your changes and finish your work by issuing the following command:
       `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`.
       Do not combine it with any other command.
       <important>After this command, you cannot continue working on this task.</important>

    ## Command execution rules

    You are operating in an environment where

    1. You issue at least one command
    2. The system executes the command(s) in a subshell
    3. You see the result(s)
    4. You write your next command(s)

    Each response should include:

    1. **Reasoning text** where you explain your analysis and plan
    2. At least one tool call with your command

    **CRITICAL REQUIREMENTS:**

    - Your response MUST include AT LEAST ONE bash tool call
    - Directory or environment variable changes are not persistent. Every action is executed in a new subshell.
    - However, you can prefix any action with `MY_ENV_VAR=MY_VALUE cd /path/to/working/dir && ...`
    - Long-running commands time out; keep individual commands focused.

    <system_information>
    {{system}} {{release}} {{version}} {{machine}}
    </system_information>

    ## Useful command examples

    ### Create a new file:

    ```bash
    cat <<'EOF' > /code/rtl/newmodule.sv
    module newmodule;
    endmodule
    EOF
    ```

    ### Edit files with sed:

    ```bash
    # Replace all occurrences
    sed -i 's/old_string/new_string/g' filename.sv

    # Replace all occurrences in lines 1-10
    sed -i '1,10s/old_string/new_string/g' filename.sv
    ```

    ### View file content:

    ```bash
    # View specific lines with numbers
    nl -ba filename.sv | sed -n '10,20p'
    ```
  step_limit: 40
  cost_limit: 0
  wall_time_limit_seconds: 0
environment:
  cwd: /code
  timeout: 300
  env:
    PAGER: cat
    MANPAGER: cat
    LESS: -R
    PIP_PROGRESS_BAR: 'off'
    TQDM_DISABLE: '1'
model:
  cost_tracking: ignore_errors
  observation_template: |
    {%- if output.output | length < 10000 -%}
    {
      "returncode": {{ output.returncode }},
      "output": {{ output.output | tojson }}
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- else -%}
    {
      "returncode": {{ output.returncode }},
      "output_head": {{ output.output[:5000] | tojson }},
      "output_tail": {{ output.output[-5000:] | tojson }},
      "elided_chars": {{ output.output | length - 10000 }},
      "warning": "Output too long."
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- endif -%}
  format_error_template: |
    Tool call error:

    <error>
    {{error}}
    </error>

    Every response needs to use the 'bash' tool at least once to execute commands.

    Call the bash tool with your command as the argument:
    - Tool: bash
    - Arguments: {"command": "your_command_here"}

    If you want to end the task, please issue the following command:
    `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`
    without any other command.
  model_kwargs:
    drop_params: true
    temperature: 0.6
    top_p: 0.95
    max_tokens: 8192
    timeout: 600
